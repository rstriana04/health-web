const endPoint = 'http://localhost';
const apiPrefix = 'api';
const webSocketEndPoint = 'ws://localhost';
export const environment = {
  apiPrefix,
  apiUrl: `${ endPoint }`,
  webSocketEndPoint,
  production: true
};
